import XCTest
@testable import CompositeImageAsset

final class CompositeImageAssetTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CompositeImageAsset().text, "Hello, World!")
    }
}
